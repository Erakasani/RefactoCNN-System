[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "refactocnn"
version = "0.1.0"
description = "RefactoCNN-System: CNN-based refactoring opportunity prediction for Java code."
requires-python = ">=3.10"
